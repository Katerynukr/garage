{% extends 'base.html.twig' %}

{% block title %}Create New Mechanick{% endblock %}

{% block body %}

<div class="container">
   <div class="row justify-content-center">
       <div class="col-md-8">
           <div class="card fraim">
               <div class="card-header head-color">
                    <h1>Create New Mechanick</h1>
                </div>
               <div class="card-body bck">
                     <form action="{{path('mechanick_store')}}" method="POST">
                        <div class="form-group">
                                <label>Name:</label>
                                <input type="text" class="form-control" name="mechanick_name">
                                <small class="form-text text-muted">Mechanick name.</small>
                            </div>

                            <div class="form-group">
                                <label>Surname:</label>
                                <input type="text" class="form-control" name="mechanick_surname">
                                <small class="form-text text-muted">Mechanick name.</small>
                            </div>
                            <input type="hidden" name="token" value="{{ csrf_token('check_csrf_hidden') }}"/>
                            <button type="submit" class="save-btn btn btn-block">Add new mechanickr</button>
                        </div>   
                    </form>
               </div>
           </div>
       </div>
   </div>
</div>
 
{% endblock %}